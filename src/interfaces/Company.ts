interface Company {
    slug?: string,
    name?: string,
    address?: string,
    city?: string,
    state?: string,
    zip?: string,
    contact?: string,
    itemsNumber?: number,
    account?: number
}

export default Company;
